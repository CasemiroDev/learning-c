#include <stdio.h>
// This library has the most commom functions in c, like "printf"

int x = 3;
//Every code will finish with semicolon

int main() {
    //Every code will has the main function, is where the code start.

    printf("Hello, world!");
    // "printf()" this function will return in the terminal the value inside the "( )"

    return 0;
    // The main function has "int" as declaration and needs this to close the function (a integer number)
}