#include <stdio.h>

int main() {
    // constants can't be changed and is good practice be in uppercase
    const int NUM = 5;
    // if you try to change will not work
    // Ex.:
    // num = 6;
    
    return 0;
}