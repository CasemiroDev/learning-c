#include <stdio.h>

int main() {

    printf("\\Hello, world!\\   \n\t");
    printf("\'I'm turning better\'\n");
    // The "printf()" function will return in the terminal all the values declared inside "( )"
    // Every time using double quotes to strings or declarations inside "( )"
    // The \n broke the line
    // Crate a horizontal line as "Tab"
    // \\ Insert a "\" on the text, without conflict
    // \' Insert a quote on the text, without conflict



    return 0;
}